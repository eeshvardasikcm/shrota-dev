package com.eeshvardasikcm.krishna.regression.testing.shrota.language

/**
 * Validator may typically be
 * returned from either ` Comparator` or `ActivatorOrModifier`.
 * These two classes are the only two classes that currently work with `Validator`.
 * There may be some additional `ShrotaLanguage` classes defined later,
 * and if there is, at that time `Validator` may possibly work with them also.
 */
internal enum class Validator {
    /**
     * Validator FavorProductivity code
     */
    FavorProductivity
}